apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: videoclub-prod
resources:
  - ../../base
  - namespace.yaml
  - ingress.yaml

nameSuffix: -prod

patches:
  - path: configmap-patch.yaml
  - path: secrets-patch.yaml
